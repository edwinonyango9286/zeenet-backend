<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Zeenet e-commerce</title>
    <style type="text/css">
      /* Base */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: "Gowun Batang", sans-serif;
        font-size: 14px;
        line-height: 1.25;
        background-color: #fafafa;
        color: #222222;
      }
      a {
        color: #000;
        text-decoration: none;
      }
      h1 {
        font-size: 20px;
        font-weight: 600;
        line-height: 1;
        margin-top: 0;
        margin-bottom: 14px;
        text-align: center;
      }
      p {
        margin-top: 0;
        margin-bottom: 24px;
      }
      table td {
        vertical-align: top;
      }
      /* Layout */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
      }
      .email-header {
        background-color: #131921;
        padding: 24px;
        color: #ffffff;
      }
      .email-body {
        padding: 24px;
        background-color: #ffffff;
      }
      .email-footer {
        background-color: #f6f6f6;
        padding: 24px;
      }
      /* Buttons */
      .button {
        display: inline-block;
        background-color: #131921;
        color: #ffffff;
        font-size: 14px;
        font-weight: 600;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-header">
        <h1>Low Stock Alert</h1>
      </div>

      <div class="email-body">
        <p>
          Hello <%= admin.name.charAt(0).toUpperCase() +
          admin.name.slice(1).toLowerCase() %>,
        </p>
        <p>The following products are low in stock and need to be restocked:</p>
        <ol>
          <% productsWithQuantityInStockLessThan5.forEach(product => { %>
          <li>
            <strong><%= product.name %></strong>: Current stock is
            <strong><%= product.quantityInStock %></strong>
          </li>
          <% }); %>
        </ol>
        <p>Please take the necessary actions to restock these items.</p>
      </div>

      <div class="email-footer">
        If you have any additional questions or need further assistance in the
        meantime, please feel free to reply to this email or contact our
        customer support team at
        <a href="mailto:technologieszeenet@gmail.com"
          >technologieszeenet@gmail.com</a
        >
      </div>
    </div>
  </body>
</html>
